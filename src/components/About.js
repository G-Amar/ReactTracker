import { Link } from "react-router-dom"

const About = () => {
  return (
    <div>
      <h4>This is an SPA created with React, JSON server, and React-router-dom</h4>
      <Link to="/">Go Back</Link>
    </div>
  )
}

export default About